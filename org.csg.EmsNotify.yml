app-id: org.csg.EmsNotify
runtime: org.kde.Platform
runtime-version: '6.8'
sdk: org.kde.Sdk
command: EmsNotify
finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --share=network
  - --device=dri
modules:
  - name: emsnotify
    buildsystem: cmake
    sources:
      - type: archive
        url: https://github.com/harsha-deep/EmsNotify/archive/refs/tags/1.0.tar.gz
        sha256: REPLACEME